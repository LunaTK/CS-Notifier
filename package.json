{
  "name": "CSNotice",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "dependencies": {
    "dotenv": "^7.0.0",
    "mongodb": "^3.3.3",
    "nodemailer": "^6.3.1",
    "nodemailer-smtp-transport": "^2.7.4",
    "pug": "^3.0.1",
    "request": "^2.88.0"
  },
  "devDependencies": {
    "eslint": "^5.16.0"
  },
  "scripts": {
    "start": "node index.js",
    "encrypt": "openssl aes-256-cbc -a -salt -in .env -out .env.enc",
    "decrypt": "openssl aes-256-cbc -a -d -in .env.enc -out .env"
  }
}
