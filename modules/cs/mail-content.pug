h1= title

each val, index in news
  h3(style={display:'inline'})= `[${val._id}] ${val.time}`
  br
  a(href=`https://cs.skku.edu/news/${type}/view/${val._id}`)
    h3(style={display:'inline'})= `${val.title}`
  br
  br